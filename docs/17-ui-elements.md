# Issue #17: Create Reusable Design System UI Elements

## Goal
Create reusable component primitives for toolbar controls.

## Added
- `ToolButtonComponent` standalone button component
- used in `WhiteboardComponent` for `Clear` and `Reset`

## Result
- small design system component introduced
- ready for additional controls (shape tool, export, etc.)
