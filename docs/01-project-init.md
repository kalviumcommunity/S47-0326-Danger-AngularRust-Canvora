# Whiteboard Backend (Rust)

- Rust project initialized (Cargo)
- To do: Add Actix/Axum, DB connection, REST, WebSocket

# Whiteboard Frontend (Angular)

- Angular project initialized
- To do: Add routing, WebSocket client, UI components

# Next Steps
- Backend: Setup dependencies, DB config
- Frontend: Setup base modules, connect to backend
