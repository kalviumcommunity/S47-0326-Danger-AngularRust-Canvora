# Issue #13: Build Core Whiteboard UI Layout Framework

## Goal
Develop a basic but functional whiteboard page layout as core UI framework.

## Changes
- `WhiteboardComponent` now includes:
  - toolbar sidebar
  - clear button
  - status text
  - full canvas area
- Layout uses CSS grid and responsive widths
- Added `clearCanvas()` method

## Result
- Clean board UI structure for upcoming real-time features.
- Easy place to add controls: color, eraser, modes.
